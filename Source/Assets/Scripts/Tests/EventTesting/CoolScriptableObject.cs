using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[CreateAssetMenu(fileName = "OWO", menuName = "OWO/CreateEVERYTHING")]
public class CoolScriptableObject : ScriptableObject
{
    public float owoLevel = 100f;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
